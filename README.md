# 🎉 NFT Gifts Market - Мини приложение с UID системой и админ панелью

## ✅ Что реализовано

### 1. UID система в профиле ✅
- **Уникальный идентификатор**: Каждый пользователь получает 8-символьный UID (например: LARGYRST)
- **Отображение в профиле**: UID показывается в профиле мини приложения под именем пользователя
- **Автоматическая генерация**: UID создается при первом входе пользователя в систему
- **Уникальность**: Система проверяет уникальность каждого UID

### 2. Админ панель как вкладка ✅
- **Интеграция в мини приложение**: Админ панель встроена как отдельная вкладка "🔧 Админ"
- **Контроль доступа**: Вкладка видна только администратору с ID: **8566238705**
- **Полный функционал**: Все возможности управления пользователями доступны через вкладку

### 3. Управление по UID ✅
- **Пополнение баланса**: Админ может пополнять баланс пользователей, указывая их UID
- **Поиск пользователей**: Система находит пользователей по UID и показывает их данные
- **Валидация**: Проверка корректности UID (8 символов, заглавные буквы и цифры)

## 🚀 Деплой на Render

### Шаг 1: Создайте новый репозиторий на GitHub
1. Создайте новый репозиторий на GitHub
2. Скопируйте все файлы из этой папки в ваш репозиторий

### Шаг 2: Подключите к Render
1. Зайдите на [render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите ваш GitHub репозиторий
4. Render автоматически обнаружит `render.yaml` и настроит деплой

### Шаг 3: Проверьте работу
После деплоя проверьте:
- Откройте мини приложение в Telegram
- Перейдите в профиль - должен отображаться UID
- Если вы админ (ID: 8566238705) - должна быть видна вкладка "🔧 Админ"

## 📁 Структура файлов

```
├── mini_app.py                    # Основное приложение с UID системой
├── render.yaml                    # Конфигурация Render
├── requirements_deploy.txt        # Зависимости Python
├── render_deploy/
│   └── init_db.py                # Инициализация БД с UID
└── templates/mini_app/
    ├── base.html                 # Базовый шаблон с навигацией
    ├── profile.html              # Профиль с отображением UID
    ├── admin.html                # Админ панель
    ├── index.html                # Главная страница
    ├── create.html               # Создание сделки
    ├── deals.html                # Список сделок
    └── deal.html                 # Просмотр сделки
```

## 🔧 API эндпоинты

- `GET /api/user_profile?user_id={telegram_id}` - получение профиля с UID
- `GET /api/admin/stats` - статистика для админ панели
- `GET /api/admin/users` - список всех пользователей с UID
- `POST /api/admin/add_balance` - пополнение баланса по UID

## 🎯 Функциональность

### Для обычных пользователей:
- Профиль с UID
- Создание и просмотр сделок
- Отображение баланса

### Для администратора (ID: 8566238705):
- Дополнительная вкладка "🔧 Админ"
- Статистика платформы
- Управление пользователями по UID
- Пополнение балансов

## 🔐 Безопасность

- Админ панель доступна только пользователю с ID 8566238705
- UID генерируются случайно и уникальны
- Все операции логируются
- Проверка существования пользователей перед операциями

---

**🎊 Система готова к использованию!**

После деплоя на Render у вас будет полнофункциональное мини приложение с UID системой и админ панелью.