# 🚀 Инструкция по деплою на Render

## 📋 Пошаговая инструкция

### Шаг 1: Подготовка GitHub репозитория

1. **Создайте новый репозиторий на GitHub**
   - Зайдите на [github.com](https://github.com)
   - Нажмите "New repository"
   - Назовите репозиторий (например: `nft-gifts-market`)
   - Сделайте его публичным
   - Нажмите "Create repository"

2. **Загрузите файлы в репозиторий**
   - Скопируйте все файлы из папки `github_deploy_files` в ваш репозиторий
   - Сделайте коммит и пуш:
   ```bash
   git add .
   git commit -m "feat: добавлена UID система и админ панель"
   git push origin main
   ```

### Шаг 2: Деплой на Render

1. **Зайдите на Render**
   - Откройте [render.com](https://render.com)
   - Войдите в аккаунт или зарегистрируйтесь

2. **Создайте новый Web Service**
   - Нажмите "New +"
   - Выберите "Web Service"
   - Подключите ваш GitHub аккаунт
   - Выберите репозиторий с мини приложением

3. **Настройка автоматическая**
   - Render автоматически обнаружит файл `render.yaml`
   - Все настройки будут применены автоматически:
     - Build Command: `pip install -r requirements_deploy.txt`
     - Start Command: `gunicorn --bind 0.0.0.0:$PORT mini_app:app`
     - Python Version: 3.11.0

4. **Запустите деплой**
   - Нажмите "Create Web Service"
   - Дождитесь завершения деплоя (обычно 2-5 минут)

### Шаг 3: Проверка работы

1. **Получите URL приложения**
   - После деплоя Render предоставит URL вида: `https://your-app-name.onrender.com`

2. **Обновите настройки бота**
   - В настройках Telegram бота укажите новый URL для мини приложения

3. **Протестируйте функции**
   - Откройте мини приложение в Telegram
   - Проверьте отображение UID в профиле
   - Если вы админ (ID: 8566238705) - проверьте админ панель

## ⚠️ Важные моменты

### База данных
- Render автоматически создаст файл `data/unified.db`
- При первом запуске выполнится инициализация БД
- UID будут генерироваться автоматически для новых пользователей

### Админ панель
- Доступна только пользователю с ID: **8566238705**
- Если нужно изменить админ ID, отредактируйте файл `templates/mini_app/base.html`

### Обновления
- При каждом пуше в GitHub, Render автоматически переделоит приложение
- Изменения применяются в течение 2-5 минут

## 🧪 Тестирование API

После деплоя можете протестировать API:

```bash
# Получение профиля пользователя
curl "https://your-app-name.onrender.com/api/user_profile?user_id=123456789"

# Статистика админ панели
curl "https://your-app-name.onrender.com/api/admin/stats"

# Список пользователей
curl "https://your-app-name.onrender.com/api/admin/users"
```

## 🆘 Решение проблем

### Если деплой не удался:
1. Проверьте логи в Render Dashboard
2. Убедитесь что все файлы загружены в репозиторий
3. Проверьте синтаксис в `render.yaml`

### Если админ панель не видна:
1. Убедитесь что ваш Telegram ID = 8566238705
2. Очистите кэш браузера
3. Перезагрузите мини приложение

### Если UID не отображается:
1. Проверьте что база данных инициализировалась
2. Попробуйте создать нового пользователя
3. Проверьте логи приложения

---

**🎉 После успешного деплоя у вас будет работающее мини приложение с UID системой и админ панелью!**