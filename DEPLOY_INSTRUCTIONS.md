# 🚀 Инструкции по деплою NFT Gifts Market

## 📁 Содержимое репозитория

Эта папка содержит **ТОЛЬКО** необходимые файлы для работы системы:

### 🎯 Основные файлы:
- `mini_app.py` - Telegram мини приложение с UID системой
- `app_new.py` - Основной Telegram бот
- `bot_full_verification.py` - Бот с системой верификации
- `init_db.py` - Инициализация базы данных
- `requirements.txt` - Python зависимости
- `render.yaml` - Конфигурация для Render

### 📂 Папки:
- `templates/mini_app/` - HTML шаблоны мини приложения
- `static/` - CSS стили
- `modules/` - Модули бота (пользователи, ошибки)
- `utils/` - Утилиты и конфигурация

## 🚀 Деплой на Render

### 1. Загрузите в GitHub
```bash
# Скопируйте ВСЕ файлы из этой папки в ваш GitHub репозиторий
git add .
git commit -m "feat: NFT Gifts Market с UID системой и админ панелью"
git push origin main
```

### 2. Создайте Web Service на Render
1. Зайдите на [render.com](https://render.com)
2. Нажмите "New" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Render автоматически обнаружит `render.yaml` и настроит деплой

### 3. Инициализируйте базу данных
После успешного деплоя выполните в консоли Render:
```bash
python init_db.py
```

### 4. Настройте конфигурацию
Отредактируйте `utils/config.ini` с вашими данными:
```ini
[telegram]
api_id = YOUR_API_ID
api_hash = YOUR_API_HASH
bot_token = YOUR_BOT_TOKEN
admin_id = YOUR_ADMIN_ID
```

## ✅ Что будет работать

### 🎁 Мини приложение:
- **URL**: `https://your-app.onrender.com`
- **Главная страница**: Создание и просмотр сделок
- **UID система**: Каждый пользователь получает уникальный 8-символьный ID
- **Админ панель**: Встроенная HTML панель для администратора
- **Ссылки на сделки**: `https://t.me/your_bot/app?startapp=deal_12345678`

### 🤖 Telegram бот:
- **Основной бот**: `app_new.py` - базовая функциональность
- **Бот с верификацией**: `bot_full_verification.py` - расширенная версия
- **Интеграция с мини приложением**: Автоматическая синхронизация данных

### 🔧 Админ функции:
- **Статистика системы**: Количество пользователей, балансы, сделки
- **Пополнение балансов**: По UID пользователя
- **Управление пользователями**: Просмотр всех пользователей с их данными

## 🧪 Тестирование

### После деплоя проверьте:
1. **Главная страница**: `https://your-app.onrender.com`
2. **Админ панель**: `https://your-app.onrender.com/admin`
3. **API статистики**: `https://your-app.onrender.com/api/admin/stats`
4. **Создание сделки**: Через мини приложение
5. **Ссылки на сделки**: Параметр `startapp` должен работать

### Локальное тестирование:
```bash
pip install -r requirements.txt
python init_db.py
python mini_app.py
# Откройте http://localhost:3000
```

## 🔧 Настройка Telegram бота

### 1. Получите токен бота
1. Напишите [@BotFather](https://t.me/BotFather)
2. Создайте нового бота: `/newbot`
3. Получите токен и добавьте в `config.ini`

### 2. Настройте мини приложение
1. В [@BotFather](https://t.me/BotFather) выберите вашего бота
2. Нажмите "Bot Settings" → "Menu Button"
3. Укажите URL: `https://your-app.onrender.com`

### 3. Получите API данные
1. Зайдите на [my.telegram.org](https://my.telegram.org)
2. Создайте приложение
3. Получите `api_id` и `api_hash`

## 🎯 Структура проекта

```
├── mini_app.py              # Мини приложение Flask
├── app_new.py               # Основной Telegram бот
├── bot_full_verification.py # Бот с верификацией
├── init_db.py               # Инициализация БД
├── requirements.txt         # Зависимости
├── render.yaml              # Конфигурация Render
├── templates/mini_app/      # HTML шаблоны
│   ├── base.html           # Базовый шаблон с навигацией
│   ├── index.html          # Главная страница
│   ├── admin.html          # Админ панель
│   ├── profile.html        # Профиль пользователя
│   ├── create.html         # Создание сделки
│   ├── deals.html          # Список сделок
│   └── deal.html           # Просмотр сделки
├── static/
│   └── style.css           # CSS стили
├── modules/                # Модули бота
│   ├── users/              # Работа с пользователями
│   └── errors/             # Обработка ошибок
└── utils/                  # Утилиты
    ├── config.ini          # Конфигурация
    └── config.py           # Загрузка конфигурации
```

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- ✅ Работающее мини приложение на Render
- ✅ UID система для всех пользователей
- ✅ Админ панель с полным функционалом
- ✅ Корректно работающие ссылки на сделки
- ✅ Интеграция с Telegram ботом

---

**🔗 Полезные ссылки:**
- [Render Documentation](https://render.com/docs)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)